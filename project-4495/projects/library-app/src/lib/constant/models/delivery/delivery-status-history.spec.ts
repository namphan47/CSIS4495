import { DeliveryStatusHistory } from './delivery-status-history';

describe('DeliveryStatusHistory', () => {
  it('should create an instance', () => {
    expect(new DeliveryStatusHistory()).toBeTruthy();
  });
});
